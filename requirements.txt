# Target: Python 3.12 (3.11 per spec not installed; 3.12 nearest ML-supported)
fastapi==0.115.0
uvicorn[standard]==0.32.0
# typed config loaded from .env (app/core/config.py) - NOT pulled in by fastapi
pydantic-settings==2.5.2
# PDF text extraction (pure-Python, simple)
pypdf==5.1.0
# Model inference. NOT sentence-transformers/torch: those cost ~356 MB of
# resident memory purely to import, and the deploy target caps a service at
# 512 MB - measurements and the reasoning are in app/core/onnx_backend.py.
# Same two checkpoints, exported by scripts/export_onnx.py (which is the only
# thing that still needs torch, and it runs at build time, not in the server).
onnxruntime==1.20.1
# Rust tokenizer, loaded from the tokenizer.json emitted by the export. This is
# the piece transformers used to provide.
tokenizers==0.22.1
# Direct dependency now: pooling and normalisation are done here (onnx_backend)
# rather than inside sentence-transformers.
numpy==2.1.3
# Postgres driver + pgvector type adapter for Python
psycopg[binary]==3.2.3
# Connection pooling. Separate package with its own version line, so it is
# pinned here rather than hidden behind the psycopg[pool] extra. Needed because
# the production database is in another region: see app/core/db.py.
psycopg-pool==3.2.4
pgvector==0.3.6
httpx==0.28.1
# BM25 keyword scoring (app/retrieval/bm25.py). ~200 lines of pure Python, no
# index server: right size for 1110 chunks, wrong one for millions (see module).
rank-bm25==0.2.2
# tests. httpx (above) doubles as the transport for fastapi's TestClient.
pytest==8.3.3
