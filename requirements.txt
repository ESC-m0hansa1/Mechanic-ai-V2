# Target: Python 3.12 (3.11 per spec not installed; 3.12 nearest ML-supported)
fastapi==0.115.0
uvicorn[standard]==0.32.0
# typed config loaded from .env (app/core/config.py) - NOT pulled in by fastapi
pydantic-settings==2.5.2
# PDF text extraction (pure-Python, simple)
pypdf==5.1.0
# local embedding model runner (pulls torch); model: BAAI/bge-small-en-v1.5
sentence-transformers==3.3.1
# Postgres driver + pgvector type adapter for Python
psycopg[binary]==3.2.3
pgvector==0.3.6
httpx==0.28.1
# BM25 keyword scoring (app/retrieval/bm25.py). ~200 lines of pure Python, no
# index server: right size for 1110 chunks, wrong one for millions (see module).
rank-bm25==0.2.2