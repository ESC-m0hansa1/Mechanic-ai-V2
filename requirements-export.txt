# Build-time / dev only: exporting the ONNX graphs and verifying the port.
#
# This is where torch lives now. It is NOT in requirements.txt because the
# server does not need it - keeping it out is the entire point of the ONNX
# backend (see app/core/onnx_backend.py for the memory measurements).
#
#   pip install -r requirements-export.txt
#   python scripts/export_onnx.py
#   python scripts/check_parity.py     # asserts onnx == torch numerically
-r requirements.txt
# Install this from PyTorch's CPU index, NOT PyPI:
#   pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu
# PyPI's linux torch is the CUDA build (~2.5 GB of NVIDIA libraries that a
# CPU host can never use). The Dockerfile's exporter stage does this first.
torch==2.13.0
transformers==4.57.6
# check_parity.py compares against the original stack, so it needs the thing
# being replaced.
sentence-transformers==3.3.1
# quantize_dynamic, for the optional --int8 export path
onnx==1.17.0
